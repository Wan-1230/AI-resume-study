# 演示与验收脚本（DEMO）

给两类场景用：**自己回归**（改完跑一遍，确认没穿帮）和 **给别人看**（15 分钟演示，每步都能当场点开）。
当前站点是本地项目，还没有公开演示地址；对外可用地址在下方"部署"一节补齐后再填进本文档。

## 0. 开始前的准备（别跳过，两处会当场翻车）

```bash
# 1) 后端 + 前端
cd backend && npm start          # http://localhost:3001
npm run dev                      # http://localhost:5173（项目根目录）

# 2) 预热：Render 免费档 15 分钟无流量会休眠；本地嵌入模型首次加载也要几秒
curl -s http://localhost:3001/api/health

# 3) 验活：确认 AI 是真的能用，而不是正在静默降级
curl -s http://localhost:3001/api/health | grep -o '"llm_status":"[^"]*"'
```

- `llm_status` 必须是 `ready`。若是 `unreachable`/`disabled`，先看 `llm_error`（常见是上游 429 配额）：
  **演示期间不要同时跑批量脚本**（`scripts/repair-questions.js` 会抢同一份额度）。
- 要用 ChromaDB 后端就先起服务端（见 [CHROMA_SETUP.md](./CHROMA_SETUP.md)），否则 `auto` 会用 memory，两者检索结果一致。

### 演示账号

| 用途 | 账号 | 密码 |
|---|---|---|
| 普通用户（有收藏/练习记录/自建题，数据非空） | `demo@example.com` | `Demo123456` |
| 管理员 | 取 `backend/.env` 的 `ADMIN_EMAIL` / `ADMIN_PASSWORD` | 同左 |

> 业务数据落在 Postgres（由 `DATABASE_URL` 指定，本地可指向 docker 起的实例，免费部署用 Neon）。想从干净状态演示：对该库执行 `DROP SCHEMA public CASCADE; CREATE SCHEMA public;`，重启后端会自动重建全部表。

## 1. 12 步演示脚本

| # | 操作 | 期望看到 | 想说明什么 |
|---|---|---|---|
| 1 | 打开首页，滚动五屏 | WebGL 背景、打字机演示、无报错 | 门面 |
| 2 | 进 `/chat`，问"RAG 召回率低应该怎么排查？" | **逐段流式**输出，正文按 Markdown 渲染，下方"参考来源"卡片带相似度分数 | RAG 端到端可用；流式不是整段返回 |
| 3 | 接着追问"那它和微调该怎么选？" | 正确把"它"理解为 RAG，回答风格口语化 | 多轮上下文真的进了提示词 |
| 4 | 点开来源卡片外链 | 能跳到原始文章 | 答案可溯源，不是"AI 说的" |
| 5 | 故意问一个库里没有的问题（如"1321 年的 Java 内存模型"） | 回答明确表示知识库中没有相关内容（而非硬编） | 拒答优于编造；阈值策略见 PRD P1-2 |
| 6 | `/practice`：选分类 + 20 题，开始 | 顶部显示"历史练习 N 次 · M 题 · 正确率 X%"和薄弱分类 | 练习数据来自库里，不是写死 |
| 7 | 答完交卷 | 结算页显示"已保存到练习记录（累计 …）" | 结果真的入库 |
| 8 | **刷新页面** | 统计数字仍在；再开一轮，选项顺序与上次不同 | 持久化 + 选项乱序 |
| 9 | `/question/q1` | 显示"大家做了 N 次，答对 X%"（没人做过时显示"还没人做过"） | 指标是真实聚合，不是 `浏览 1.2k` 这种假数 |
| 10 | 点收藏 ⭐ → 刷新 → 退出重登 | 收藏状态保持；换未登录账号则看不到 | 用户维度数据隔离 |
| 11 | `/my-questions` 新建一题 + `/import` 传一份含错误行的 CSV | 新题出现在列表且刷新仍在；导入横幅显示"成功 X 道，Y 道被跳过"并列出每行原因 | CRUD 与导入都是真的，失败不静默 |
| 12 | `/resume`（登录后）粘贴 JD + 简历，开始优化 | 流式输出优化稿；未登录时提示"请先登录"而不是"后端没启动" | 最贵的调用要求登录；错误提示指向正确原因 |

可选加演（若被问到工程性）：
```bash
cd backend
node scripts/audit-questions.js          # 题库质量：答案分布 64/64/64/63、占位干扰项 0
curl -s localhost:3001/api/health        # 向量后端、embedding 版本、LLM 状态
```

## 2. 已知"看着像 bug、其实不是"

| 现象 | 原因 | 说明口径 |
|---|---|---|
| 回答变成"⚠️ AI 生成不可用（429 …），下面直接给知识库原文" | 上游免费档速率限制 | 不是检索坏了；换端点或降并发即可，前端已明确标出原因 |
| 首页/问答能开，但简历优化报"请先登录" | 设计如此：简历是敏感数据且最贵的调用 | 登录后可用 |
| 登录页没有 GitHub 按钮 | 未配置 `GITHUB_CLIENT_ID` 时前端主动隐藏（探测 `/api/auth/providers`） | 比给一个必错按钮更诚实 |
| 第一次提问比预期慢几秒 | 主因是 LLM 生成，不是检索：嵌入模型已在 `backend/.cache`，加载 0.5s，检索本身约 7ms（首问 0.35s 后进入热态） | 检索耗时可忽略，慢的是模型首 token |
| 管理员只有一个账号、且来自 `.env` | 没有管理员 CRUD | 已知边界，PRD 未列入本期 |
| 用 `curl -d '{"message":"中文"}'` 测 `/api/chat`，回答先反问"你是不是乱码了" | Git Bash / Windows 命令行把请求体编码搞坏了，服务端解码没问题 | 改成 `--data-binary @q.json`（文件用 UTF-8 存的 JSON）再测；浏览器里不会有这问题 |
| 题目难度几乎全是 medium | 故意没重标定，等有真实正确率再回填 | 见 PRD P0-4 未做说明 |

## 3. 回归清单（每次改完跑一遍，约 5 分钟）

```bash
npm run check && npm run lint            # 类型 + 静态检查
cd backend && node --check server.js     # 后端语法
npm run build                            # 前端能构建产物
```
再手工过演示脚本的 2、7、8、11 四步（流式、入库、跨刷新、失败回报）。

## 4. 部署后待补

- [ ] 填入前端与后端的公开地址，并把本节改成"线上地址"
- [ ] 生产 `JWT_SECRET` 换成长随机值、`ADMIN_PASSWORD` 不再是默认值
- [ ] 若上生产：GitHub OAuth App 的回调地址改成线上域名，并实测一次成功回路
- [ ] 若多实例部署：`backend/guard.js` 的内存限流需换成共享存储
